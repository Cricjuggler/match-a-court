{
  "installCommand": "cd .. && npm install",
  "buildCommand": "cd .. && npm run build:business",
  "outputDirectory": "business/dist",
  "framework": "vite",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
